#!/bin/bash
# Script to optimize all animations by removing y transforms

# This script will be used to batch replace y transforms with opacity-only animations
# For now, we'll do it manually per file

echo "Optimizing animations..."
